<UserControl x:Class="SorterControls.Views.Entities.EntityHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:steps="clr-namespace:SorterControls.DesignData.Steps"
             d:DataContext="{d:DesignInstance steps:DesignSorterPoolStepVm, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <DockPanel LastChildFill="True">

        <Grid Background="#FF946D7E">

            <ContentControl Content="{Binding }"
                ContentTemplateSelector="{StaticResource entityTemplateSelector}" >

            </ContentControl>

        </Grid>
    </DockPanel>

</UserControl>
